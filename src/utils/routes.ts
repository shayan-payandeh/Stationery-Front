const adminRoute = "/admin";

const categoriesRoute = `${adminRoute}/categories`;
const categoryAdd = `${categoriesRoute}/add`;
const categoryEditRoute = `${categoriesRoute}/edit`;

export const adminRoutes = {
  adminRoute,
  categoriesRoute,
  categoryAdd,
  categoryEditRoute,
};
